export enum PageConstant {
  // basic login path
  BASE_LOGIN = '/login',
  // basic home path
  BASE_HOME = '/home',
  // error page path
  ERROR_PAGE = '/exception',
}

// sidebar extion
export enum action {
  tables,
  people,
  project,
  calendar,
  timesheet,
  reports,
  rooms,
  subject,
}

// role or auth type
export enum role {
  admin,
  asdos,
  student,
  guest,
}
